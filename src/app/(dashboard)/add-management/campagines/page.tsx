import CampaginTables from '@/views/admanagement/campagins/tables/CampaginTables'
import React from 'react'

function Page() {
  // await new Promise(resolve => setTimeout(resolve, 6000)) // 2 seconds

  return <CampaginTables />
}

export default Page
